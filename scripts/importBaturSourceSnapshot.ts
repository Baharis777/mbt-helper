import { existsSync, mkdirSync, writeFileSync } from 'node:fs';
import path from 'node:path';

const importsDirectory = path.join(process.cwd(), 'knowledge', 'imports');
const snapshotPath = path.join(importsDirectory, 'batur-source-snapshot.md');
const notesPath = path.join(importsDirectory, 'batur-import-notes.md');

const sourcePageUrl = 'https://mybalitrips.com/ru/climbing-to-batur-volcano/';
const anchors = ['1906', '2095', '1728', '1008', '1905', '1009', '2094'];

const snapshotContent = `# Импорт страницы Батура

## Инструкция
Вставить сюда вручную скопированный текст со страницы MyBaliTrips и каждого варианта/опции тура.

## Основная страница
source_url: ${sourcePageUrl}
raw_text: NEEDS_INPUT

${anchors
  .map(
    (anchor) => `## Вариант #${anchor}
source_url: ${sourcePageUrl}#${anchor}
exact_site_name: NEEDS_INPUT
raw_text: NEEDS_INPUT`,
  )
  .join('\n\n')}
`;

const notesContent = `# Заметки по ручному импорту Батура

## Назначение

Этот файл описывает ручной процесс импорта страницы Батура. Скрипт не скрапит сайт автоматически и не подтверждает факты. Менеджер вручную копирует текст со страницы MyBaliTrips в \`knowledge/imports/batur-source-snapshot.md\`, а затем обновляет матрицу вариантов.

## Что скопировать с сайта

- Основной текст страницы про Батур.
- Заголовок каждого варианта или опции тура.
- Ценовой блок каждого варианта.
- Описание программы каждого варианта.
- Что включено.
- Что не включено.
- Правила трансфера и районы посадки.
- Длительность и время выезда, если указаны.
- Правила отмены, оплаты или бронирования, если показаны.
- Упоминания горячих источников, купален или термальных источников.
- Указание, является ли вариант групповым или приватным.
- Возрастные, медицинские или физические ограничения, если они показаны.

## Как определить точные названия вариантов

1. Открыть страницу ${sourcePageUrl}.
2. Найти вариант по якорю: #1906, #2095, #1728, #1008, #1905, #1009, #2094.
3. Скопировать видимый заголовок варианта без сокращений.
4. Вставить его в поле \`exact_site_name\` соответствующего раздела snapshot-файла.
5. Если заголовок не удается однозначно сопоставить с якорем, оставить \`NEEDS_INPUT\` и добавить заметку рядом с raw_text.

## Какие факты требуют ручного подтверждения

- Цена.
- Валюта.
- Наличие мест.
- Длительность.
- Время выезда.
- Что включено.
- Что не включено.
- Точная программа.
- Трансфер и районы посадки.
- Горячие источники или купальни.
- Приватный или групповой формат.
- Правила отмены, оплаты и бронирования.
- Ограничения по возрасту, здоровью и физической нагрузке.
- Любой текущий статус безопасности, открытости маршрута или доступности.

## Как обновлять \`variant-matrix.md\`

1. Сначала заполнить \`knowledge/imports/batur-source-snapshot.md\`.
2. Для каждого якоря сопоставить \`exact_site_name\` с временным \`variant_id\`.
3. Перенести в \`knowledge/tours/batur-sunrise/variant-matrix.md\` только те факты, которые явно есть в скопированном тексте или подтверждены менеджером.
4. Если факт не найден или формулировка неоднозначна, оставить \`NEEDS_REVIEW\`.
5. После матрицы обновлять отдельные файлы \`knowledge/tours/batur-sunrise/variants/*.md\`.
6. Не переносить факты одного варианта в другой.

## Важно про безопасность

Статическая страница сайта не подтверждает текущую безопасность Батура. Даже если на странице описан тур, это не значит, что маршрут сейчас открыт, безопасен или доступен.

Любой вопрос про извержение, активность вулкана, пепел, дым, лаву, землетрясение, закрытие маршрута, текущую безопасность или доступность требует ручной актуальной проверки у организатора перед подтверждением клиенту.

## TODO для менеджера

- [ ] Скопировать текст основной страницы.
- [ ] Скопировать название каждого варианта.
- [ ] Скопировать ценовой блок каждого варианта.
- [ ] Скопировать включения и исключения.
- [ ] Скопировать правила трансфера и районов посадки.
- [ ] Скопировать правила отмены, оплаты или бронирования, если они показаны.
- [ ] Проверить, включены ли горячие источники.
- [ ] Проверить, является ли вариант групповым или приватным.
- [ ] Проверить, показаны ли возрастные ограничения.
- [ ] Проверить, показаны ли ограничения по здоровью или физической нагрузке.
- [ ] Оставить \`NEEDS_REVIEW\` для любого неподтвержденного факта.
`;

mkdirSync(importsDirectory, { recursive: true });
createFileIfMissing(snapshotPath, snapshotContent);
createFileIfMissing(notesPath, notesContent);

function createFileIfMissing(filePath: string, content: string): void {
  if (existsSync(filePath)) {
    console.log(`Файл уже существует, не перезаписываю: ${toProjectPath(filePath)}`);
    return;
  }

  writeFileSync(filePath, content, 'utf8');
  console.log(`Создан файл: ${toProjectPath(filePath)}`);
}

function toProjectPath(filePath: string): string {
  return path.relative(process.cwd(), filePath).split(path.sep).join('/');
}
